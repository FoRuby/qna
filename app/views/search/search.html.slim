.search_results
  .row
    .col
      h3 Search results:
      - if @search_results.empty?
        span.result Nothing was found.

  - @search_results.each do |result|
    hr
    =render partial: "search/#{result.class.name.downcase}", locals: { result: result }
  hr

div id=answer.id
  span= answer.body
  - if current_user&.author?(answer)
    = link_to 'Edit answer',
               '#',
               class: 'edit-answer-link',
               data: { answer_id: answer.id }

    = link_to 'Delete answer',
               answer_path(answer),
               method: :delete,
               data: { confirm: "Are you sure?" }

    = render template: 'answers/edit',
             locals: { answer: answer }

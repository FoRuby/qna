$('.edit-question-errors')
  .html('<%= render 'shared/errors', resource: @question %>');
<% if @question.errors.blank? %>
  $('.question').replaceWith(
    '<%= j render 'questions/show_question', resource: @question %>'
  );
  $('.flash-messages').html('<%= j render 'shared/messages' %>');
<% end %>
